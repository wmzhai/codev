interface:
  display_name: "memorize"
  short_description: "为新项目构建并持续刷新面向 Codex 的项目记忆体系"
  default_prompt: "使用 $memorize 审查当前仓库，并以简体中文构建或刷新它的 AGENTS.md 与 memory/ 记忆体系；如果存在 CLAUDE.md，则把 repo 事实并入 AGENTS.md，但保留仍然服务宿主代理或 gstack 的兼容说明。"

policy:
  allow_implicit_invocation: true
