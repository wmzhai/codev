interface:
  display_name: "ships"
  short_description: "轻量提交推送当前分支并可选创建 release 或 rc 标签"
  default_prompt: "使用 $ships 走轻量 commit/push/tag 流程；如果仓库同时使用 gstack，则把它视为不接管 PR、review gate 或 repo 级文档同步的 fallback。"

policy:
  allow_implicit_invocation: false
